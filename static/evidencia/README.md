Las capturas originales de `main` estaban versionadas con Git LFS:

- `spark_master.png`
- `minio_bucket.png`

Al restaurarlas sin descargar LFS solo se obtienen punteros de texto, no las
imagenes reales. Para volver a mostrarlas en `/arquitectura`, recupera los
binarios con Git LFS o reemplazalos por capturas nuevas del despliegue actual.
